# LOWG -- Graz Airport

Primary test airport. Complex enough to exercise most features.

## Physical layout (apt.dat)

- **3 runways:** 16C/34C (3000m, asphalt, 45m wide, main), 16R/34L (grass, 25m), 16L/34R (grass, 30m)
- **Displaced thresholds:** 16C has 260m displaced threshold
- **6 helipads:** H1-H6
- **96 taxi nodes**, **75 taxi edges**, **86 active zones**
- **~60 stands** (mostly tie-down, some gates)
- **26 taxi signs**, **3 lighting objects** (PAPI for 16C/34C)
- **Tower viewpoint** at record 14
- **ATC flows:** Northern Flow (wind 080-260) and Southern Flow (wind 260-080), each with runway assignments for all 3 runway pairs
- **Pattern runway:** 16C left-hand (from record 1101)

## Operational data (OFMX)

- **Elevation:** 1120ft
- **Magnetic variation:** 5 degrees (2022)
- **Transition altitude:** 10,000ft
- **CTR:** Class D, SFC-4500ft AMSL (code LO585)
- **ctrApproximationRadius:** 18 NM (max polygon edge 16.25 NM rounded UP + ~1 NM ARP-proxy-offset margin, circular sim approximation). Source: AIP Austria, AD 2 LOWG §2.17 (control zone lateral limits), effective 2026-04-01 (AIRAC 2604), via Austro Control / OpenFlightMaps Austria mirror at [data/ofm/austria/ofmx_extracted/ofmx_lo/isolated/ofmx_lo.ofmx](/home/andrew/dev/projects/twr2/data/ofm/austria/ofmx_extracted/ofmx_lo/isolated/ofmx_lo.ofmx). Polygon ranges 6.7–16.25 NM from ARP. Rounding up + proxy margin keeps the rule from releasing inside the polygon under worst-case threshold-proxy offset. Real CTR is polygonal; the circular value is a stand-in pending polygon containment (`D-AUDIT-polygon-ctr`).
- **TMA layers:** LOWG 1-5 with Class C/D/E segments up to FL245
- **Frequencies:** GRAZ TOWER 118.200, GRAZ RADAR 119.300/120.440, ATIS 126.130
- **VFR reporting points:** GRAZ-NORD (MRP), SENDER DOBL, KALSDORF, AUTOBAHN-OST, AUTOBAHN-WEST, GREEN CITY (all RP), GLEISDORF (MRP), LASSNITZHÖHE (MRP)
- **Navaids:** GRZ VOR/DME (116.20)

## IFR procedures (CIFP)

- **SIDs (21):** ABIR3V/4G/5U, GBG5Y/7X, GOLV4U/5G, GOTA5G/5U, GRZ4X/4Y, MILG3H/5U/6G, MURE4U/5G, RADL4V/5G/6U, ROPA4G/4U -- for RW16C and RW34C
- **STARs (8):** ABIR1M, GBG1M, GOLV1M, GOTA2M, LEOB1M, MURE1M, RADL2M, RUPE2M -- all coded for ALL runways
- **Approaches (5 types):** D16C (VOR/DME), D34C (VOR/DME), I34C (ILS cat III), R16C (RNAV), R34C (RNAV) -- with multiple approach transitions each
- **ILS:** OEG localizer on 34C, cat III
- **Runway thresholds:** 16C at N47000722/E015261181 elev 1117ft, 34C at N46584003/E015263581 elev 1088ft
- **Structured IFR inventory:** The generated LOWG structured package now carries this CIFP procedure inventory as an explicit candidate IFR block, together with CIFP runway-threshold positions, a fix-resolution scan against checked-in OFMX designated points, navaids, and chart coding tables, compiled SID/STAR candidate routes, and tower-scope default approach candidates for `D16C`, `D34C`, `I34C`, `R16C`, and `R34C`.

## Known gaps

- **Circuit geometry:** Not in any downloadable data source. apt.dat gives direction (left-hand for 16C). OFM renders accurate circuits in their tiles but geometry is not exported. Needs hand-authoring.
- **VFR routes:** OFMX has the reporting points but not the route geometry connecting them. OFM map shows routes labelled PRC-1 through PRC-5 (PRC-4/5 appear to be the circuit patterns). Route geometry needs hand-authoring.
- **IFR fix positions:** LOWG IFR identifier resolution is now effectively complete for v1. All `46` distinct CIFP identifiers resolve from checked-in sources or conservative CIFP-derived approach geometry: `GOTAR` from OFMX designated points, `GBG` and `GRZ` from parsed OFMX navaids, `35` identifiers from local IFR chart coding tables, and the remaining approach-structure identifiers from CIFP-derived geometry anchored on known runway/navaid positions.
- **Runtime IFR projection:** The current LOWG runtime candidate now includes a first IFR subset:
  - the LOWG SID set
  - the LOWG STAR set
  - `VOR RWY 16C`
  - `VOR RWY 34C`
  - `RNP RWY 16C`
  - `RNP RWY 34C`
  - `ILS RWY 34C`
  - the shared `LOWG_GBG_MISSED_HOLD`
  Remaining IFR content stays outside the current-core subset: `LOC 34C` and richer published minima variants still live in the structured package only.

## CAD authoring status

- **Ground DXF:** `cad/airports/lowg.dxf` aligns well with the X-Plane LOWG airport geometry when fitted to the full 16C/34C runway strip from `apt.dat`. Most authored holding/taxi geometry lands close to the apt.dat taxi network. The main intentional divergence is the `16/34L` glider-use area and the taxiway link toward `16/34C`, which should not be treated as an unresolved suspect.
- **Ground naming evidence:** X-Plane `apt.dat` for LOWG includes both taxi-route records and location/sign records. Those records support treating the tower-side component around marker `1` as a mixed `D -> A` cluster instead of forcing it into a single taxiway label.
- **Circuit DXF:** `cad/airports/lowg_circuits.dxf` is in a separate local frame/scale from the ground DXF. The fit now explicitly uses the two user-confirmed centre-runway anchor points from the sidecar as runway-end controls, with the third marker landing near the X-Plane tower viewpoint as a cross-check.
- **Circuit topology:** The current circuit drawing behaves like a shared graph rather than four independent closed loops. One main component contains the centre-runway circuit structure, and the two side components terminate exactly on segments of that main component, implying the importer should support splitting a segment where another authored path endpoint lands on it.
- **Directional circuit projection:** That shared graph now projects into eight runway-specific `CircuitProcedure` entities in the current-core candidate: `16C/34C` east and west variants, `16R/34L` west-side variants, and `16L/34R` east-side variants. The current compiler is still LOWG-specific rather than a general airport-agnostic circuit solver, but the current candidate no longer omits circuits.
- **Charted VFR route join:** The NE reporting-point route now targets an explicit authored `NE` circuit anchor in `lowg_circuits.dxf`, which is the current stand-in for the charted eastern holding join.
- **Circuit alignment status:** Under the current fit, the centre-runway circuit spine passes through `16C/34C` exactly, and the main `16R/34L` and `16L/34R` parallel legs have both been deliberately nudged onto their runway axes as local fudges and trimmed inward to make the turns read more naturally. The turn transitions around those side-runway legs are now approximated as short line-sampled curves rather than abrupt stubs, while the runway attachments remain exact.
- **DXF semantics:** Both DXFs currently use only `LINE` and `POINT` entities on a single geometry layer, with no embedded labels. Semantic naming therefore has to live in sidecar data.
- **Ground naming workflow:** Holding-point names live in the marker mappings, and taxiway / ground-path names are now scaffolded separately in `namedMappings.groundComponents` so component-level geometry can be named without overloading the point markers.
- **Parking entity source:** The current LOWG structured package no longer takes stands from the visible X-Plane parking set. It now takes stand geometry from `NEW_Parking_Points` and apron geometry from `NEW_Parking`, both read from [lowg_working_combined.dxf](/home/andrew/dev/projects/twr2/cad/airports/lowg_working_combined.dxf). X-Plane parking remains reference/reconciliation data only, mainly for secondary attributes like stand type and aircraft hints.
- **VFR route scaffold:** The LOWG sidecar now captures the intended reporting-point paths `GLEISDORF -> LASSNITZHÖHE -> AUTOBAHN-OST -> circuit (NE entry)`, `SENDER DOBL -> AUTOBAHN-WEST -> GREEN CITY -> GRAZ-NORD`, `SENDER DOBL -> circuit (SW entry)` as the alternate western join, and `KALSDORF -> circuit (SE entry)` as explicit ordered path definitions. For the reporting-point portions, the geometry is simply the literal join between the existing mapped VFR reporting points. The circuit side is now anchored by an explicit `NE` point, an explicit `SW` point, the existing `SE` endpoint, and `AUTOBAHN-WEST` as the `NW` point.
- **Sector-boundary status:** The current working DXF sketch for `SECTOR WHISKEY` and `SECTOR ECHO` is now treated as user-confirmed geometry and anchors. The charts indicate that `SENDER DOBL` and `KALSDORF` sit on the CTR intersection. Those sectors now project into the runtime candidate as first-class operational sectors, and the worked low-level CTR/TMA slice now projects with them; the remaining surrounding-airspace gap is now the broader non-runtime special-use set plus altitude-aware membership.
- **Structured operational sectors:** The LOWG manifest now carries `SECTOR WHISKEY` and `SECTOR ECHO` as structured VFR operational-sector objects linked to the working-DXF boundaries, CTR-boundary anchors (`SENDER DOBL` / `KALSDORF`), `PRC-2` / `PRC-3`, contact-before-entry semantics, and the published `3000 ft MSL` ceiling. These now project into the structured airport package, the current-core world candidate, and the strict plate view model.
- **Published VFR procedure scaffold:** The LOWG sidecar now also captures OFM `PRC-1`, `PRC-2`, `PRC-3`, and `PRC-5` as published VFR procedures separate from geometry. This is where entry-point protocol such as contact-before points, altitude caps, route-end holds, reporting expectations, published waypoint labels, and comm-failure exit paths belongs.
- **Structured published procedures:** Those LOWG published-procedure notes are now promoted into structured procedure objects in the generated [structured-airport-package.json](/home/andrew/dev/projects/twr2/cad/airports/rendered/lowg/structured-airport-package.json). `PRC-1` to `PRC-5` and `ARR-1` now also project into runtime-owned `AerodromeAip.publishedVfrProcedures` for the LOWG current-core candidate.
- **Typed AIP projection boundary:** The current-core LOWG projection now narrows that publication data into typed runtime shapes: `PlateId`, explicit contact timing, sealed published point / sector-anchor references, grouped advisories, and explicit communication-failure structure. Raw publication scaffold status and provenance remain in the structured airport package rather than leaking into `core`.
- **Additional OFM content scaffold:** The LOWG sidecar now also carries OFM `PRC-4` west circuit, `PRC-5` east/west-by-ATC availability note, `ARR-1` Gleisdorf arrival/NORDO references, and briefing content from `AD-3` / `AD-4` / `AD-5` / `AD-6`, plus `AD-1` taxi-layout context, `ENR-1` reference-box content, and `AD-2` apron/parking content. For the current version-1 fit, `AD-2` now draws from the authored parking graph and authored stand points rather than the old X-Plane stand inventory.
- **Holding-model mismatch:** The current runtime/FM model encodes orbit/holding geometry as closed paths, but that is not the intended long-term semantics. LOWG holding-area authoring should be treated as provisional until the loiter-volume model is restored.
- **Runway-source divergence:** 34C agrees between `apt.dat`, OFMX, and CIFP to within sub-metre noise. 16C differs by about `258.5m` between the `apt.dat` strip end and the OFMX/CIFP threshold position, and 16R/34L differs by about `150m` per end between `apt.dat` and OFMX, consistent with displaced-threshold effects.
- **Report scaffold:** `cad/airports/lowg.manifest.json` and `bin/airport_authoring_report.py` now provide a repeatable LOWG reconciliation pass across `apt.dat`, both DXFs, CIFP, and OFMX.
- **Render scaffold:** `bin/render_airport_authoring.py` now emits SVG overlays plus a single interactive HTML map under `cad/airports/rendered/lowg/`. The map supports layer toggles, pan/zoom, and airport/airspace/full extents views, overlays LOWG airspace boundaries from OFMX, draws sidecar-defined VFR route paths between the existing reporting points and declared circuit anchors, can show the normalized `VFR Op Sectors` working overlay, exposes the circuit/procedure anchors as their own layer, shows a dedicated `X-Plane parking access via taxiway A` layer, and keeps marker/label/stroke sizing readable across zoom levels.
- **Generated plate pack:** `bin/render_airport_plate.py` now emits a first LOWG plate pack under [rendered/lowg/plate/index.html](/home/andrew/dev/projects/twr2/cad/airports/rendered/lowg/plate/index.html), with generated SVG page maps and a printable multi-page HTML document mirroring the OFM page set. It already covers `AD-1`, `AD-6`, `AD-3`, `AD-4`, `AD-5`, `PRC-1` to `PRC-5`, `ENR-1`, `AD-2`, and `ARR-1`. In the current v1 pass, `PRC-5` remains explicitly provisional because east-hold geometry is still pending, while `AD-2` now uses the authored parking graph / authored stand-point set with direct authored attachments onto taxiway `A`, and the PRC / ARR pages now render structured candidate procedure objects from the LOWG package plus their linked candidate routes, sectors, and circuit graphs.
- **Entity-only plate boundary:** The LOWG plate generator now has an explicit anti-corruption layer in [airport_plate_view_model.py](/home/andrew/dev/projects/twr2/bin/airport_plate_view_model.py). That translator projects the structured LOWG airport package plus the validated current-core subset into a dedicated plate view model, and [render_airport_plate.py](/home/andrew/dev/projects/twr2/bin/render_airport_plate.py) now renders only from that view model. In the current pack, `AD-1`, `AD-6`, and `AD-2` still draw their aerodrome layers from the validated current-core subset in [world-candidate.json](/home/andrew/dev/projects/twr2/cad/airports/rendered/lowg/world-candidate.json), but the procedure and briefing pages are now intentionally sparse and gap-heavy because publication-supplement content is no longer allowed to leak into the plates.
- **Structured airport package artifact:** The intended layering in [structured-airport-package.md](/home/andrew/dev/projects/twr2/docs/design/structured-airport-package.md) now exists in code and output form. [airport_structured_package.py](/home/andrew/dev/projects/twr2/bin/airport_structured_package.py) plus [project_structured_airport_package.py](/home/andrew/dev/projects/twr2/bin/project_structured_airport_package.py) now emit [structured-airport-package.json](/home/andrew/dev/projects/twr2/cad/airports/rendered/lowg/structured-airport-package.json), which is the explicit pre-core structured truth for LOWG.
- **Entity-fit audit:** [lowg-entity-fit.md](/home/andrew/dev/projects/twr2/cad/airports/lowg-entity-fit.md) now records how far the current LOWG package can be projected into `AviationWorld` / `Aerodrome`. The current runtime slice now projects runways, taxiways, stands, aprons, fixes, VFR routes, operational sectors, published VFR procedures, runway-specific directional circuits, and the worked low-level LOWG CTR/TMA slice into the current-core candidate. The main remaining gaps are broader surrounding-airspace projection, altitude-aware airspace membership, and deferred hold semantics.
- **Projected entity bundle:** `bin/project_airport_entities.py` now emits [entity-bundle.json](/home/andrew/dev/projects/twr2/cad/airports/rendered/lowg/entity-bundle.json), but that file is now a compatibility projection derived from the structured airport package instead of the primary structured truth. This keeps the older entity-bundle path available while shifting the authoritative airport representation one layer earlier.
- **Route-airspace slice widened:** Runtime `VfrRouteAirspaceProfile` now supports `InVolume`, `InClass`, and volume-authoritative segmented legs. For LOWG, `vfr_southeast_entry_path` and `vfr_southwest_entry_path` now project as `InVolume(LO585)`, `vfr_western_corridor_path` now projects as a segmented `LO585 -> LO0EF_E` route with an explicit boundary transition point, and `vfr_northeast_entry_path` is still left unassigned where the pre-CTR leg cannot yet be mapped to a runtime volume honestly.
- **Current-core world candidate:** `bin/project_airport_world_candidate.py` now emits [world-candidate.json](/home/andrew/dev/projects/twr2/cad/airports/rendered/lowg/world-candidate.json), a filtered LOWG subset shaped to the current `AviationWorld` model rather than to the richer projection bundle.
- **Shared candidate schema:** The JSON shape for that current-core candidate no longer lives only inside the LOWG JVM test. It now has a shared Kotlin schema in [WorldCandidateSchema.kt](/home/andrew/dev/projects/twr2/migration/src/commonMain/kotlin/xyz/easiersaid/twr/migration/world/WorldCandidateSchema.kt), which the validation harness consumes directly.
- **Current-core validation result:** `migration/src/jvmTest/kotlin/xyz/easiersaid/twr/migration/world/LowgWorldCandidateValidationTest.kt` now validates that filtered LOWG subset and writes [world-validation-report.json](/home/andrew/dev/projects/twr2/cad/airports/rendered/lowg/world-validation-report.json). The current result is clean again: it now reports `0` issues and `0` structural issues. The earlier `UNREACHABLE_STAND_FROM_HOLDING_POINT` failures turned out to be an interpretation bug in the parking compiler, not a geometry bug in the DXF. The fix was to split `NEW_Parking` at real line-line intersections and to treat direct authored touches onto taxiway `A` as shared graph attachment points instead of routing everything through the older three projected branch joins. That clean pass is now conditional on a worked low-level 2D point-to-volume airspace assignment rather than the old synthetic point-claim CTR volume, and on the runway-protection holding points projected only onto the connected `A` taxiway spine using the snapped `D` marker plus X-Plane sign evidence for `G1/G2`, `X`, and `Y`. Circuit projection is no longer part of the current omission set.
- **Holding-point nuance:** The richer LOWG entity bundle still retains the disconnected `B`, `C`, `Y`, and `Z` holding candidates as candidate-only data rather than importing them directly into the current-core subset. Under the present validator, disconnected runway-side holding points would also trigger stand-reachability failures, so the clean current-core pass should be read as “LOWG now fits the current model under explicit current-core assumptions,” not as “all LOWG holding semantics are solved.”
- **Plate-generation direction:** [entity-driven-plates.md](/home/andrew/dev/projects/twr2/docs/design/entity-driven-plates.md) now makes the intended layering explicit: plates should be driven from a strict plate view model translated out of the structured airport package and current-core subset, with explicit gap pages rather than supplement leakage.
- **Migration-to-core handoff:** [airport-migration-core-gap.md](/home/andrew/dev/projects/twr2/docs/design/airport-migration-core-gap.md) is now the actionable write-up of what LOWG has already proven at the migration/package layer, what still fails because of the current `core` model, and which parts are projection-only rather than true model gaps.
- **Ad hoc airspace DXF:** `bin/export_airspace_dxf.py` now exports [lowg_airspace_working.dxf](/home/andrew/dev/projects/twr2/cad/airports/lowg_airspace_working.dxf), a LibreCAD-oriented working file containing OFMX airspace boundaries, VFR reporting points, runway context, and tower context in the shared local meter frame.
- **Normalized airspace DXF:** `bin/normalize_airspace_dxf.py` now produces [lowg_airspace_working_normalized.dxf](/home/andrew/dev/projects/twr2/cad/airports/lowg_airspace_working_normalized.dxf) from the edited working file by fitting the labeled VFR reporting points back onto the OFMX reference points.
- **X-Plane baseline DXF:** `bin/export_xplane_baseline_dxf.py` now exports [lowg_xplane_baseline.dxf](/home/andrew/dev/projects/twr2/cad/airports/lowg_xplane_baseline.dxf), a bootstrap LibreCAD file generated directly from `apt.dat` runway/taxi-route/sign/parking data plus OFMX VFR reporting points. This does not depend on the hand-authored LOWG DXFs and is the cleaner starting point for future airport geometry authoring.
- **Combined working DXF:** `bin/export_airport_working_dxf.py` now exports [lowg_working_combined.dxf](/home/andrew/dev/projects/twr2/cad/airports/lowg_working_combined.dxf), which overlays the X-Plane / OFMX baseline layers with the transformed hand-authored LOWG ground geometry, VFR circuit, procedure anchors, and current `VFR Op Sectors`. For LOWG specifically, this is the pragmatic bridge between the baseline-first workflow and the hand-authored work already invested.
- **Working DXF default:** [lowg_working_combined.dxf](/home/andrew/dev/projects/twr2/cad/airports/lowg_working_combined.dxf) now embeds the raster OSM underlay by DXF image reference on layer `REF_OSM_RASTER`, already aligned in the local airport frame. Vector OSM layers are still omitted by default unless `bin/export_airport_working_dxf.py --include-vector-osm` is requested explicitly.
- **Collaborative `NEW_*` layers:** The combined LOWG DXF is now also the collaboration surface for temporary hand-authored overlays. `bin/export_airport_working_dxf.py` preserves existing `NEW_*` layers across rebuilds so those overlays are not wiped during regeneration.
- **OSM underlay bridge:** `bin/export_osm_geojson_dxf.py` now supports both local-GeoJSON input and direct Overpass fetch. In direct mode it derives a local LOWG bounding box from `apt.dat`, fetches the OSM underlay classes, caches the result to [lowg_osm_underlay.geojson](/home/andrew/dev/projects/twr2/cad/airports/lowg_osm_underlay.geojson), and projects the underlay into [lowg_osm_underlay.dxf](/home/andrew/dev/projects/twr2/cad/airports/lowg_osm_underlay.dxf). This remains a visual/context underlay path rather than a semantic aviation-data source.
- **Raster underlay path:** `bin/render_osm_underlay_raster.py` now renders the cached LOWG OSM extract into [lowg_osm_underlay.png](/home/andrew/dev/projects/twr2/cad/airports/lowg_osm_underlay.png), with exact local-frame placement data in [lowg_osm_underlay_placement.json](/home/andrew/dev/projects/twr2/cad/airports/lowg_osm_underlay_placement.json). This is now the preferred CAD underlay path.
- **NEW_Parking fit:** The first `NEW_Parking` apron/parking geometry pass is encouraging. The current LOWG working DXF contains `39` parking-segment lines, and against the `56` visible X-Plane parking positions the nearest-segment fit is `42/56` within `5m`, `48/56` within `15m`, and `54/56` within `30m`, with the `A/G1` side already very tight and the rougher residuals concentrated on the `A/S1` and `A/X` sides. The naive endpoint-only graph reading is also misleading: `25` of the apparent open endpoints already lie exactly on another parking segment as point-on-segment joins, which matches the current LibreCAD drawing style.
- **NEW_Parking_Points:** The working DXF now also contains `38` point entities on layer `NEW_Parking_Points`. All of them lie exactly on the authored `NEW_Parking` geometry, so they are usable as the authored stand-point geometry set. The LOWG manifest now carries the definitive row-order naming for those `38` points, and the structured package / current-core candidate now project them as the primary stand set.
- **Taxi-sign overlay:** The interactive LOWG map can now show `apt.dat` taxi signs as a separate layer, which exposes the X-Plane taxiway naming/signage evidence while working through the sidecar assignments.
- **Airspace boundary limitation:** LOWG 2 contains OFMX `FNT` boundary vertices. The current map renders that boundary as a straight-segment approximation and does not yet reconstruct the curved geometry.
- **DXF export limitation:** The ad hoc airspace DXF uses the same straight-segment approximation for OFMX `FNT` boundary vertices. The new `VFR Op Sectors` layer can carry a manual CTR cut-out sketch and is now visible in the interactive map, but that geometry is not yet integrated into the runtime/model.
- **Workboard:** airport-specific authoring status and next steps live in `cad/airports/lowg-authoring.md`.
